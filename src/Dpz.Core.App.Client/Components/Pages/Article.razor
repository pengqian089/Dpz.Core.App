@page "/article"

<MudStack Class="article-page" Spacing="2">
    <MudPaper Class="top-bar pa-3" Elevation="1" Square="true">
        <MudStack Spacing="2">
            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
                <MudText Typo="Typo.h6" Class="page-title">发现文章</MudText>
                <MudIconButton Icon="@Icons.Material.Filled.Refresh" Color="Color.Primary" Disabled="_isRefreshing || _initialLoading" OnClick="RefreshAsync" />
            </MudStack>
            <MudTextField @bind-Value="_searchText" Placeholder="搜索标题或内容" Immediate="true" Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" OnBlur="(()=> OnSearchAsync(_searchText))" OnKeyDown="@(async e=> { if(e.Key=="Enter") await OnSearchAsync(_searchText); })" />
            @if (_tags?.Count > 0)
            {
                <div class="tags-scroll">
                    <MudChipSet T="string" Mandatory="false" MultiSelection="false" Class="d-flex">
                        <MudChip T="string" Color="Color.Primary" Variant="Variant.Filled" Class="@(_currentTag==null ? "tag-chip active" : "tag-chip")" OnClick="(()=>OnSelectTagAsync(null))">全部</MudChip>
                        @foreach (var tag in _tags)
                        {
                            <MudChip T="string" Color="Color.Primary" Variant="@(_currentTag==tag ? Variant.Filled : Variant.Outlined)" Class="@(_currentTag==tag ? "tag-chip active" : "tag-chip")" OnClick="(()=>OnSelectTagAsync(tag))">@tag</MudChip>
                        }
                    </MudChipSet>
                </div>
            }
        </MudStack>
    </MudPaper>

    @if (_isRefreshing)
    {
        <MudProgressLinear Class="refresh-bar" Color="Color.Primary" Indeterminate="true" />
    }

    @if (_initialLoading)
    {
        <MudStack Spacing="2" Class="pa-2">
            @for (int i = 0; i < 6; i++)
            {
                <MudPaper Class="article-item skeleton-card" Elevation="0">
                    <div class="media-wrapper skeleton-media">
                        <MudSkeleton Width="100%" Height="100%" />
                    </div>
                    <div class="content-wrapper pa-3">
                        <MudSkeleton Width="70%" Height="20px" Class="mb-2" />
                        <MudSkeleton Width="100%" Height="14px" Class="mb-1" />
                        <MudSkeleton Width="90%" Height="14px" Class="mb-2" />
                        <MudSkeleton Width="50%" Height="12px" />
                    </div>
                </MudPaper>
            }
        </MudStack>
    }
    else if (_source.Count == 0)
    {
        <MudPaper Class="empty-block pa-6" Elevation="0">
            <MudStack Spacing="1" AlignItems="AlignItems.Center">
                <MudIcon Color="Color.Tertiary" Icon="@Icons.Material.Filled.Article" Size="Size.Large" />
                <MudText Typo="Typo.subtitle1">暂无文章</MudText>
                <MudText Typo="Typo.caption" Color="Color.Tertiary">试试更换标签或关键字</MudText>
            </MudStack>
        </MudPaper>
    }
    else
    {
        <MudStack Spacing="2" Class="article-list" Style="padding:8px;">
            @foreach (var item in _source)
            {
                <MudPaper Class="article-item" Elevation="1" @onclick="(()=>OnOpenArticle(item))">
                    <div class="media-wrapper">
                        @if (!string.IsNullOrWhiteSpace(item.MainImage))
                        {
                            <img src="@item.MainImage" alt="封面" class="cover-img" loading="lazy" />
                        }
                        else if (item.ImagesAddress?.Length > 0)
                        {
                            <div class="thumbs-row">
                                @foreach (var img in item.ImagesAddress.Take(3))
                                {
                                    <img src="@img" alt="图" class="thumb-img" loading="lazy" />
                                }
                            </div>
                        }
                        else
                        {
                            <div class="no-image placeholder-gradient">
                                <MudIcon Icon="@Icons.Material.Filled.Image" Color="Color.Tertiary" />
                            </div>
                        }

                        @if (item.ImagesAddress?.Length > 3)
                        {
                            <MudChip T="string" Class="more-images-chip" Color="Color.Dark" Variant="Variant.Filled" Size="Size.Small">+@(item.ImagesAddress.Length - 3)</MudChip>
                        }
                    </div>

                    <div class="content-wrapper pa-3">
                        <MudStack Spacing="1">
                            <MudText Typo="Typo.subtitle1" Class="title truncate-2">@item.Title</MudText>
                            <MudText Typo="Typo.body2" Color="Color.Tertiary" Class="intro truncate-3">@item.Introduction</MudText>

                            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Class="meta-row">
                                <MudStack Row="true" Spacing="1" Class="tags-inline">
                                    @foreach (var tag in item.Tags ?? [])
                                    {
                                        <MudChip T="string" Color="Color.Primary" Variant="Variant.Text" Size="Size.Small" Class="mini-tag">@tag</MudChip>
                                    }
                                </MudStack>
                                <MudText Typo="Typo.caption" Color="Color.Tertiary">@FormatRelativeTime(item.CreateTime)</MudText>
                            </MudStack>

                            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.FlexStart" Class="stats-row" Spacing="2">
                                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Class="stat">
                                    <MudIcon Icon="@Icons.Material.Filled.Visibility" Size="Size.Small" />
                                    <MudText Typo="Typo.caption">@item.ViewCount</MudText>
                                </MudStack>
                                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Class="stat">
                                    <MudIcon Icon="@Icons.Material.Filled.ChatBubbleOutline" Size="Size.Small" />
                                    <MudText Typo="Typo.caption">@item.CommentCount</MudText>
                                </MudStack>
                                @if (item.Author != null)
                                {
                                    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="1" Class="author-block">
                                        <MudAvatar Size="Size.Small" Color="Color.Primary">
                                            <MudImage Src="@item.Author.Avatar"></MudImage>
                                        </MudAvatar>
                                        <MudText Typo="Typo.caption" Class="author-name truncate-1" >@item.Author?.Name</MudText>
                                    </MudStack>
                                }
                            </MudStack>
                        </MudStack>
                    </div>
                </MudPaper>
            }
        </MudStack>

        <div class="load-more-container" id="infinite-scroll-sentinel">
            @if (_isLoadingMore)
            {
                <MudProgressCircular Color="Color.Primary" Indeterminate Size="Size.Medium" />
            }
            else if (_hasMore)
            {
                <MudButton Variant="Variant.Text" Disabled="_isLoadingMore" OnClick="LoadNextPageAsync" Color="Color.Primary">加载更多</MudButton>
            }
            else
            {
                <MudText Typo="Typo.caption" Color="Color.Tertiary">没有更多了</MudText>
            }
        </div>
    }
</MudStack>